# Menu props (Explore 3D)

Drop **Godot 4 .glb** files here. Explore instances every real mesh (root not
`PLACEHOLDER_*`) onto the outdoor eating patio — tables, seating, and the
ground near chairs. These are **walkable map items**, not the Order screen.

3D Models textures each prop from real Sunshine photos (Square catalog
and `assets/generated/menu/`), not invented fantasy food.

## Filenames (matched to table / ground slots)

| File | Slot |
| --- | --- |
| `prop_croissant.glb` | West picnic table |
| `prop_croissant_cookie.glb` | West picnic table |
| `prop_lemonade.glb` | West picnic table |
| `prop_croissant_almond.glb` | East picnic table |
| `prop_roll.glb` | East picnic table |
| `prop_fruit_tea.glb` | East picnic table |
| `prop_loaf.glb` | North picnic table |
| `prop_loaf_garlic.glb` | North picnic table |
| `prop_milk_tea.glb` | North picnic table |
| `prop_savory.glb` | SW bistro |
| `prop_savory_bbq.glb` | SW bistro |
| `prop_matcha_latte.glb` | SW bistro |
| `prop_savory_cajun.glb` | SE bistro |
| `prop_savory_fajita.glb` | SE bistro |
| `prop_vietnamese_coffee.glb` | SE bistro |
| `prop_loaf_milk.glb` | NW bistro |
| `prop_loaf_rosemary.glb` | NW bistro |
| `prop_croissant_berry.glb` | NE bistro |
| `prop_croissant_pistachio.glb` | NE bistro |
| `prop_coffee.glb` | Center bistro (offset off the spawn walk) |
| `prop_biscoff_coffee.glb` | Center bistro |
| `prop_water.glb` | Ground beside the chalk menu board |
| `prop_savory_mushroom.glb` | Ground near cornhole |

Extra `prop_*.glb` files that do not match a slot still spawn on the patio
ground near seating (not the south-lawn spawn axis).

Until a matching GLB is present, Explore shows a low-poly pastry or cup
textured with the real bakery / Square photo on a small plate.

Units: meters, Y-up, identity attach. Origin is the mesh bottom.
